# Avel — Suivi sport

App PWA mobile-first de suivi sportif (running, vélo d'appartement, muscu, HIIT, étirements). UI en français, charte **« bord de mer » : ardoise & écume, sombre** (juillet 2026 — remplace le vert sauge/crème clair).

## Commandes

- `npm run dev` — serveur de dev (http://localhost:5173), mode cloud si `.env` rempli
- `npm run dev:demo` — serveur en mode local forcé (port 5174, `.env.demo` vide les clés Firebase)
- `npm run build` — tsc + vite build vers `dist/`
- `node scripts/smoke.mjs` — test de fumée Playwright (lancer `dev:demo` avant), captures dans `screenshots/`
- `node scripts/check-alternance.mjs` / `check-interval.mjs` / `check-v4.mjs` / `check-blocs.mjs` / `check-form-ui.mjs` / `check-player-reps.mjs` / `check-plan.mjs` / `check-planning-plan.mjs` / `check-today-plan.mjs` / `check-weekday-cycle.mjs` / `check-validate-date.mjs` / `check-validate-recent.mjs` / `check-validate-used.mjs` / `check-strava-sync.mjs` / `check-log-date.mjs` / `check-plan-velo.mjs` — vérifs Playwright ciblées (alternance, intervalle, objectifs à paliers, blocs muscu, formulaires épurés, réalisé dans le minuteur, page Plan (**séances validées masquées du flux principal → section « Terminées »**), séances du plan dans le Planning, **séances de course du plan affichées en à-faire sur Aujourd'hui**, cadence d'alternance sur jours de semaine, confirmation de date à la validation d'une séance du plan, **sélecteur de validation limité aux courses de moins d'une semaine** (injecte des `activities` dans localStorage) **et excluant les courses déjà utilisées** (`check-validate-used`), **bouton « Synchroniser depuis Strava » qui importe les courses via un Worker mocké** (`vite --mode test`, Worker intercepté par `page.route`), édition de la date d'une séance terminée), aussi sur `dev:demo`
- `node scripts/check-onDays.mjs` — test de **logique pure** (sans navigateur, via Vite `ssrLoadModule`) du scheduler `repeat.onDays` : alternance posée sur des jours de semaine choisis (lun/jeu/sam), jamais les jours de course, alternance qui continue d'une semaine à l'autre
- `node scripts/check-progression.mjs` — test de **logique pure** de `progressedSession` (progression auto des objectifs) : hausse, perf en baisse, séance interrompue, circuits multi-tours, étirements, HIIT, vélo (FC exclue), running hors périmètre
- `node scripts/check-progression-ui.mjs` — vérif Playwright bout-en-bout : 14 reps faites là où l'objectif était 12 → programme et minuteur passent à 14, séries non faites à 12, fiche de séance **non** réécrite
- `node scripts/check-warmup.mjs` — test de **logique pure** de `warmupsDueOn` (échauffement automatique `Session.warmupFor`) : invitation les jours où une séance de la catégorie cible est à faire, disparition une fois faite, pas de doublon si l'échauffement est déjà planifié
- `node scripts/check-last-perf-stop.mjs` — test de **logique pure** de `stopPoint`/`lastPerfLine` (bloc « Dernière fois ») : tour du circuit + exercice où la séance s'est arrêtée, séance complète, découpage en blocs, HIIT (intervalles), repli quand la séance a changé depuis
- `node scripts/check-rappels-agenda.mjs` — test de **logique pure** de `buildAgenda` (rappels push) : séances utilisateur + séances du plan, jours de repos omis, séances validées retirées, cycles d'alternance, empreinte stable
- `node scripts/check-rappels-worker.mjs` — test du **Worker de rappels** sans réseau ni KV réel (KV factice, `fetch` bouchonné, clés VAPID éphémères) : passage heure d'été/hiver, fenêtre de tolérance, anti-doublon par heure, **plusieurs rappels/jour et rangs annonce/relance**, migration de l'ancien format `hour`, surface HTTP, purge sur 410. Prérequis : `cd worker/notif && npm install`
- `node scripts/check-rappels.mjs` — vérif Playwright du bloc « Rappels » des Réglages (`vite --mode test`, Worker intercepté par `page.route`, `serviceWorker`/`Notification` bouchonnés)
- `node scripts/generate-vapid.mjs` — génère la paire de clés VAPID des rappels push dans `.env` (idempotent : refuse d'écraser)
- **Worker Cloudflare `worker/notif/`** (`notif-elan.js` + `due.js` + `wrangler.toml` + `README.md`) — **rappels de séance par notification** : Cron Trigger `*/5 * * * *` + Web Push. `npx wrangler deploy` après `wrangler kv namespace create REMINDERS` et `wrangler secret put VAPID_PRIVATE_KEY|ELAN_NOTIF_KEY`. Setup détaillé dans `worker/notif/README.md`
- `node scripts/check-cloud.mjs` — vérifie l'écran de connexion Google sur 5173
- `node scripts/fetch-logs.mjs` — lit les logs/séances Firestore de l'utilisateur via l'auth firebase-tools du configstore (REST, lecture seule)
- `node scripts/push-to-intervals.mjs [--week N] [--push] [--clear] [--future]` (ou double-clic sur `pousser-vers-coros.bat`, qui utilise `--clear --push --future`) — pousse le plan semi (`src/data/plan.ts`) vers **intervals.icu** en séances planifiées structurées (qui synchronise ensuite vers COROS). Dry-run par défaut ; `--push` envoie ; `--clear` purge d'abord nos séances (`external_id` préfixe `elan-`) ; **`--future` n'envoie que les séances d'aujourd'hui et à venir** (`date >= todayStr` local) — on ne planifie pas dans le passé, ça évite le bruit « conformité 0% » sur les semaines historiques de juin conservées dans `plan.ts`. **Identifiants** (clé API + Athlete ID) résolus par `scripts/intervals-creds.mjs` : env `INTERVALS_API_KEY`/`INTERVALS_ATHLETE_ID`, sinon un **fichier texte gitignoré** du dossier (ex. « user + API.txt », parseur souple `label=valeur` ou valeurs en vrac), sinon saisie interactive. Charge `plan.ts` via Vite `ssrLoadModule` (source unique, pas de duplication). La clé n'est jamais écrite sur disque ni loguée.
- `node scripts/gen-xlsx.mjs` (ou double-clic sur `generer-le-tableur.bat`) — régénère `Plan-Semi-Rennes-2026.xlsx` (racine du dossier SPORT, à côté du mémo `.md`) **à partir de `src/data/plan.ts`**, sans dépendance de tableur : le script écrit lui-même le ZIP et les XML OOXML. Charge le plan via Vite `ssrLoadModule`, comme `push-to-intervals.mjs`. Colonnes dérivées du `Workout` structuré (résumé en clair, allure et FC des étapes de travail, distance via `workoutStats`) ; la feuille « Allures & FC » dérive de `PLAN_ZONES`. Deux tables restent propres au script parce qu'elles n'existent pas dans le plan : `JOURNAL` (ce qui a réellement eu lieu, par date — colonne « Suivi », ligne « Hors plan » si aucune séance ce jour-là) et la feuille « Jour J » (stratégie de course). **Avertit en console si le `km` déclaré d'une semaine s'écarte de plus de 1,5 km de la somme de ses séances.** ⚠ Écrit par-dessus le fichier : le fermer dans Excel avant de lancer.
- ~~`pull-activities.mjs` / `recuperer-mes-courses.bat`~~ — **supprimé (juin 2026)** : la récupération des courses (COROS → Firestore `activities`) passe désormais par la **synchro Strava in-app** (bouton « Synchroniser », voir plus bas). `push-to-intervals.mjs` (push plan → montre) reste, lui, sur intervals.icu.
- `node scripts/strava-auth.mjs` (ou double-clic `autoriser-strava.bat`) — **autorisation Strava unique** : serveur localhost + fetch natif (zéro dépendance), échange le code OAuth contre un **refresh_token** à coller dans les secrets du Worker. Demande Client ID/Secret (env `STRAVA_CLIENT_ID`/`STRAVA_CLIENT_SECRET` ou saisie). Callback Strava = `localhost`.
- **Worker Cloudflare `worker/`** (`strava-elan.js` + `wrangler.toml` + `README.md`) — pont **Strava → Avel** déployé hors repo app : `npx wrangler deploy` après `wrangler secret put STRAVA_CLIENT_ID|STRAVA_CLIENT_SECRET|STRAVA_REFRESH_TOKEN|ELAN_SYNC_KEY`. Rafraîchit le token Strava côté serveur (le navigateur ne peut pas : CORS bloqué sur `/oauth/token`) et renvoie les courses récentes au format `Activity`. CORS ouvert à l'origine d'Avel. Setup détaillé dans `worker/README.md`.
- `node scripts/make-icons.mjs` — régénère les PNG PWA (pastille ardoise + ogive) dans `public/icons/`. Le script **compose lui-même son SVG** (géométrie de l'ogive en dur) : il ne part plus de `public/icon.svg`, qui est la version transparente réservée à l'écran.
- `node scripts/check-jour.mjs` — vérif visuelle de l'écran **Jour** (Aujourd'hui) à la charte bord de mer : photo + scrim, titre condensé sur deux lignes, pilules de comptage, cartes en verre, tuiles de code, section « Terminées ». Contrôle surtout que **les deux webfonts sont réellement appliquées** en lisant le style calculé (`Big Shoulders Display` sur le `h1`, `Space Mono` sur le sur-titre) — un repli silencieux sur Arial Narrow passerait inaperçu sur une capture. Captures dans `screenshots/jour-*.png`
- `node scripts/check-semaine.mjs` — vérif de l'écran **Planning** : capture au viewport (pas `fullPage`) et **mesure du débordement vertical**. Échoue si l'écran ne tient plus sur 844 px de haut ou si la dernière ligne passe sous la barre d'onglets.
- `node scripts/check-planif-ui.mjs` — vérif de la section **Planification** de la fiche séance (sept. 2026) : trois positions (plus d'« Alternance » ni de « Rotation »), **aperçu = grille du Planning** (la fiche en première ligne, « Routine matinale » à 7 anneaux, courses du plan semi si la semaine est dans le plan, pas de débordement horizontal), anneaux de la fiche qui suivent les cases L/J/S, sélecteur « En alternance avec » → HIIT (pastille avec sa croix, ligne HIIT dans l'aperçu, anneaux partagés comptés sur cette semaine + la suivante via ‹ ›), « Commencer par HIIT » (les noms) change la prochaine occurrence AVANT d'enregistrer, tout décoché → avertissement et « Non planifié » à l'enregistrement (aucune cadence cachée). Ancres : `[data-preview] [data-session]` (lignes), anneau = `span.border-2` (le `DayDot` « prévu »). Captures `screenshots/planif-*.png`.
- `node scripts/check-draft-restore.mjs` — vérif du **brouillon de la fiche séance** (sept. 2026) : nom modifié + lundi coché → « Fiche exercice » → Retour ou Enregistrer → tout est encore là ; le retour de la fiche restaurée demande confirmation ; le brouillon ne ressuscite pas à l'ouverture suivante. Captures `screenshots/draft-*.png`.
- `node scripts/check-library-ui.mjs` — vérif de l'écran **Exercices** (liste des programmes, sept. 2026) : cartes dépliables sur place (exercices + « Modifier »), un seul programme déplié à la fois, second tap = repli, « Modifier » → fiche, desktop en deux colonnes. Captures `screenshots/exercices-*.png`.
- `node scripts/make-photo.mjs [--quality 58] [--width 780]` — régénère `src/assets/bord-de-mer.webp` (le fond de l'app) depuis le master JPEG qui vit **hors app**, à la racine du dossier SPORT. Recadre à 1000 px de large centrés sur 64 % du dépassement, ce qui correspond au `background-position: 64% 30%` de `SeaBackdrop` : **les deux vont ensemble**, changer l'un impose de rejouer l'autre.
- `node scripts/check-charte.mjs` — vérif visuelle de la charte (`dev:demo` lancé) : traverse Réglages, onglet Plan, fiche COROS et Objectifs — les écrans que `smoke.mjs` ne voit pas — et capture dans `screenshots/charte-*.png`. C'est là que se contrôle l'empilement des fonds, seul vrai piège du thème sombre.

Le `.env` (gitignoré) contient la config Firebase du projet `routine-sport-ca440` de l'utilisateur. Il peut aussi porter `VITE_STRAVA_SYNC_URL`/`VITE_STRAVA_SYNC_KEY` (URL du Worker Strava + clé partagée) ; tant que l'URL est vide, le bouton « Synchroniser » reste masqué. Idem `VITE_NOTIF_URL`/`VITE_NOTIF_KEY` + `VITE_VAPID_PUBLIC_KEY` (rappels push) : URL vide → bloc « Rappels » masqué. `VAPID_PRIVATE_KEY` vit aussi dans `.env` **sans préfixe `VITE_`** (donc jamais dans le bundle) et se pose en secret sur le Worker. `.env.test` (commité, sans secret) = mode local + Workers factices pour `check-strava-sync.mjs` et `check-rappels.mjs` (`vite --mode test`).

## Déploiement

**Depuis le 1er septembre 2026, un push sur `main` publie tout seul** : le workflow `.github/workflows/firebase-hosting.yml` (GitHub Actions) fait `npm ci` → reconstruit le `.env` depuis le secret `AVEL_DOTENV` (le `.env` local sans `VAPID_PRIVATE_KEY`) → `npm run build` → `FirebaseExtended/action-hosting-deploy` sur les deux sites, avec le compte de service `github-action-avel@routine-sport-ca440.iam.gserviceaccount.com` (secret `FIREBASE_SERVICE_ACCOUNT`, rôles Hosting Admin / Auth Admin / API Keys Viewer / Cloud Run Viewer — ceux de `firebase init hosting:github`, créés à la main via l'API IAM parce que l'assistant est interactif). Suivi : `gh run list -R mhuur/Sport` / `gh run watch`. ⚠ Une nouvelle variable `VITE_*` dans le `.env` local doit être **reportée dans le secret** (depuis PowerShell : `cmd /c "gh secret set AVEL_DOTENV -R mhuur/Sport < fichier"` — jamais via un pipe PowerShell, qui corrompt l'encodage de la 1ʳᵉ ligne du secret : cause de la panne du 01/09), sinon la version en ligne ne l'a pas. Un commit **sans push** ne publie toujours rien.

Publication manuelle (secours, ou pour tester sans committer) :

```
npm run build
npx firebase-tools deploy --only hosting
```

⚠ **JAMAIS `--non-interactive` sur ce `deploy`.** Le drapeau supprime les demandes de confirmation et le classificateur de permissions de Claude Code **refuse la commande** quand il est présent. Relevé sur les transcripts du projet : les 5 déploiements sans le flag sont tous passés (29-30/07/2026), les 3 tentatives avec ont été refusées — d'où la journée du 09/08/2026 passée à croire, à tort, que la permission de déployer avait été retirée, alors que seule la commande avait changé. Diagnostic transposable : **avant de conclure qu'un droit a disparu, comparer la commande exacte à celle qui marchait**, les transcripts sont dans `~/.claude/projects/…-Elan/*.jsonl`. `publier-l-app.bat` (racine de `Sport/`) enchaîne les deux commandes pour l'utilisateur, sans le flag, et rappelle qu'il faut rouvrir la PWA **deux fois** pour dépasser le cache du service worker.

L'app vit sur **https://avel.web.app** — c'est l'URL à utiliser. Le projet Firebase garde son ID d'origine `routine-sport-ca440` (un ID de projet est **immuable**) : `avel` est un **second site Hosting** du même projet (`firebase hosting:sites:create avel`), et `firebase.json` déclare les deux sites sur le même `dist/`, si bien qu'un `npx firebase-tools deploy --only hosting` publie sur les deux. L'ancienne URL `routine-sport-ca440.web.app` reste donc valide (utile : c'est encore elle dans les PWA déjà installées).

Ce qui a dû suivre le nouveau domaine, à refaire pour toute nouvelle origine :
- `ALLOWED_ORIGINS` des **deux Workers** (`worker/strava-elan.js`, `worker/notif/notif-elan.js`) + `npx wrangler deploy` dans chaque dossier — sinon CORS bloqué sur la synchro Strava et l'abonnement aux rappels ;
- **domaines autorisés de Firebase Auth** (sinon `auth/unauthorized-domain` à la connexion Google) : ils ne suivent PAS automatiquement les sites Hosting additionnels. Ajout fait via l'API `identitytoolkit.googleapis.com/admin/v2/projects/{id}/config` (`authorizedDomains`) avec le jeton firebase-tools du configstore — même mécanisme que `scripts/fetch-logs.mjs`, à défaut console Firebase → Authentication → Settings.

Attention, **localStorage et abonnements push sont par origine** : sur un nouveau domaine, les réglages locaux (heures de rappel, ancre de la section plan, dernière synchro Strava) repartent à zéro et il faut se réabonner aux notifications. Les séances et logs, eux, sont dans Firestore : rien à migrer.

## Nom et identité

L'app s'appelait **Élan** (feuille verte) ; renommée **Avel** en juillet 2026 — breton pour « le vent ». Le **nom affiché reste Avel** ; seule l'identité visuelle a changé.

Logo (juillet 2026) : **l'ogive** — une amande verticale tendue aux deux pointes, écume dégradée sur ardoise. Un seul chemin dans un carré de 512 :
`M256 88 C 344 194, 344 318, 256 424 C 168 318, 168 194, 256 88 Z` (hauteur 336, largeur 176, rapport 1 : 1,9). Dégradé vertical `#FFFFFF → #DCE7EC` sur fond sombre, ombre douce `0 18px 34px rgba(2,10,16,.55)`. **Jamais de contour, jamais de rotation, jamais d'aplat gris intermédiaire.** (Remplace le monogramme « A taillé » corail sur encre.)

Deux formes, deux usages — l'écran d'accueil impose la pastille :
- **transparente** (`public/icon.svg`, masters dans `src/assets/` et `public/icons/elan-ogive-{ecume,ardoise}.svg`) : splash, écran de connexion, tout fond sombre ;
- **sur pastille ardoise unie** (`public/icons/*.png` + `elan-ogive-plate.svg`) : icônes PWA et favicons. Sur l'écran d'accueil iOS, l'alpha d'une icône est **écrasé en noir** — une icône transparente y apparaîtrait sur un carré noir sale. `icon-512-maskable.png` réduit le motif à 70 % pour survivre au rognage des icônes adaptatives Android.

Références aux icônes à garder synchronisées si les chemins bougent : `index.html` (favicons + apple-touch-icon + `theme-color`), `vite.config.ts` (manifest), `src/sw.ts` (icône et badge des notifications).

Seul le **nom affiché** a changé (manifest, titre, écran de connexion, splash, notifications, docs). Les identifiants techniques restent en `elan-` **exprès**, parce qu'ils sont persistés ou déployés — les renommer casserait des données ou des URL :

- clés localStorage `elan-data-v1`, `elan-seeded-*`, `elan-mig-subtypes-*`, `elan-plan-anchor-*`, `elan-rappel-*`, `elan-strava-last` → renommer = données locales perdues ;
- `planRef` des séances du plan (`elan-{date}`, `src/lib/planDay.ts`) → **écrit dans les logs Firestore existants**, c'est lui qui dit qu'une séance est validée ;
- `external_id` `elan-` des séances poussées sur intervals.icu (le `--clear` s'en sert pour retrouver les nôtres) ;
- Workers déployés `elan-notif` / `strava-elan` et leurs secrets `ELAN_NOTIF_KEY` / `ELAN_SYNC_KEY`, plus l'URL `strava-elan.saintilan.workers.dev` déjà dans le `.env`.

Les fichiers d'icônes livrés avec la charte gardent eux aussi le préfixe `elan-` (`elan-ogive-*.svg/png`) : c'est le nom du système design d'origine, pas une régression du renommage.

## Environnement

Node.js est installé en **portable** (pas de droits admin sur cette machine) dans
`%LOCALAPPDATA%\Programs\node-v24.16.0-win-x64`, ajouté au PATH utilisateur.
Si `node` est introuvable dans un shell : `$env:Path += ";$env:LOCALAPPDATA\Programs\node-v24.16.0-win-x64"`.
Ne jamais lancer d'installation nécessitant une élévation UAC (winget MSI, etc.).

## Architecture

- **Double mode de stockage** (`src/data/store.ts`) : interface `Store` avec `LocalStore` (localStorage, par défaut sans `.env`) et `FirestoreStore` (`users/{uid}/exercises|sessions|logs|ideas`). Le mode est choisi dans `src/firebase.ts` selon la présence des variables `VITE_FIREBASE_*` dans `.env`. La collection `ideas` (bugs & idées saisis dans Réglages) ne bloque jamais `dataReady` et dégrade en liste vide si les règles Firestore la refusent.
- `src/data/DataContext.tsx` : provider unique (auth Google + abonnements aux 4 collections + CRUD + seed au premier lancement via `src/data/seed.ts`, gardé par un flag localStorage `elan-seeded-*`).
- Types et métadonnées de catégories (couleurs/emoji/classes Tailwind) : `src/types.ts`.
- Jours de semaine indexés **0 = lundi … 6 = dimanche** (`src/lib/dates.ts`), dates de logs en `YYYY-MM-DD` local.
- Planification : jours fixes (`Session.days`) OU intervalle (`Session.repeat`). En intervalle, **deux cadences** : « tous les X jours » (`repeat.everyDays` + `repeat.startDate`) OU **sur des jours de semaine choisis** (`repeat.onDays: number[]`, 0=lun…6=dim ; si défini et non vide, `everyDays` est ignoré). La rotation est `repeat.steps: { ids: string[] }[]` : chaque étape regroupe les séances d'un MÊME jour (ex. [Pompes+Abdos] puis [Jambes+Dos]), `occurrence % steps.length` choisit l'étape. **Calcul de l'occurrence** (`plannedSessionIdsOn`, `src/lib/schedule.ts`) : cadence « tous les X jours » → `occurrence = diffDays(startDate, date) / everyDays` (si multiple) ; cadence `onDays` → si `date` un de ces jours de semaine et ≥ `startDate`, `occurrence = countWeekdays(startDate, date, onDays)` (nb de ces jours déjà passés), ce qui pose l'alternance UNIQUEMENT sur les jours choisis (ex. lun/jeu/sam pour éviter les jours de course du plan) tout en la faisant continuer d'une semaine à l'autre. **UI (refonte du 05/09/2026, après audit utilisateur — canvas « Planification d'un programme »)** : le « quand » est un `Seg` à trois positions « Jours choisis / Tous les X jours / Avant une autre » (état `planMode: 'weekly' | 'every' | 'warmup'` ; « Jours fixes » jusqu'au 05/09 au soir), et **l'alternance est une ligne à part, « En alternance avec »**, cumulable avec les deux premières cadences. **Simplifiée le 05/09/2026 au soir** (second retour utilisateur, canvas « Planification simplifiée » : les crans A/B, le « + » par cran « le même jour, aussi » et « + Ajouter » étaient « compliqués ») : un sélecteur natif compact « Aucune » (`select[aria-label="En alternance avec"]`) liste les autres séances ; la séance choisie devient une **pastille pleine largeur à sa couleur, avec sa croix** (`[aria-label="Retirer l'alternance"]`), sous le libellé. L'état `steps` garde sa forme générale (`string[][]`, dérivés `simpleSteps` / `partner` / `setPartner`) : un cycle existant à trois crans ou à plusieurs séances le même jour **n'est plus créable dans la fiche mais reste lu, planifié et préservé à la re-sauvegarde** — la ligne l'affiche en texte « Vélo + Full body → HIIT » avec la croix pour repartir de zéro (`check-alternance` l'injecte en localStorage et le vérifie). **Jours choisis + alternance = `repeat.onDays` + `steps`** (l'ancien mode `weekdays`, disparu de l'UI) : les cases L…D éditent le même état `days`, `scheduleWrites` l'écrit dans `days` (sans alternance) ou dans `repeat.onDays` (avec). **Un cycle n'est écrit que s'il a une cadence** : alternance sans aucun jour coché → pas de cycle, la carte dit « Non planifié » (bug du 05/09 : on enregistrait « Tous les 2 jours », la cadence de l'autre onglet, invisible à l'écran). **« Commencer par »** (les noms des séances, plus de lettres ; libellé au-dessus, `Seg compact` pleine largeur ; au-delà d'une quarantaine de signes en tout, `startOptions` abrège chaque nom en `short` — l'`aria-label` garde le nom complet, donc `getByRole('button', { name: 'HIIT — Cardio express' })` tient ; ne PAS mettre `truncate` sur les boutons du `Seg` : l'espacement des lettres fait apparaître l'ellipse même quand le texte tient) apparaît dès qu'on alterne, dans les deux cadences : état `startStep` dérivé de la phase actuelle (`countWeekdays` sur jours de semaine, `nextEveryOccurrence` en tous les X jours), réancrage de `startDate` (`backOccurrences`, ou `-k × everyDays`) seulement si le choix diffère de la phase — l'historique affiché ne bouge pas. **Aperçu** en bas de la carte = **la grille du Planning** (retour utilisateur du 05/09/2026 : la première version, un calendrier de deux semaines ne montrant que ce programme, ne permettait pas de le caler « en fonction de ce qu'on a déjà ») : une ligne par séance avec ses sept `DayDot` (composant partagé `src/components/DayDot.tsx`, extrait de `Planning.tsx`), semaine navigable ‹ › (`weekOffset` local + `usePlanningWeek`, « Cette semaine » / « Sem. du 7 sept. », en-tête lettre + numéro avec aujourd'hui en pastille), **la fiche en première ligne, teintée lagon**, puis les autres séances planifiées (même filtre que le Planning, ordre `sortOrder`, sous-libellé `CODE · ↻ cadence` pour les membres d'un cycle), puis les courses du plan semi (`planStates`, lecture seule, `CODE · plan semi`) ; ronds pleins = logs de la semaine (`isPlanLog` exclu). Calculé par `plannedSessionIdsOn` (et `warmupsDueOn` en « Avant une autre ») sur une copie des séances où la fiche et ses écritures (`scheduleWrites`, pure, partagée avec l'enregistrement) sont appliquées — une seule source de vérité, l'aperçu ne peut pas mentir. `PreviewRow` (local à `SessionForm.tsx`) = grille du Planning sans poignée, jours en 24 px (28 dans le Planning) pour laisser la place au nom dans une carte. Ligne « Prochaine fois : … » (60 j de recherche), avertissement si aucun jour choisi. `describeSchedule` affiche « Lun · Jeu · Sam » (tableau d'objets car Firestore refuse les tableaux imbriqués ; les anciens champs `alternates`/`alternateWith` sont normalisés par `cycleStepsOf()`). Le cycle est porté par UNE séance « propriétaire » ; les membres se détectent via `ownerOf()` et l'éditent depuis leur fiche (réécriture du propriétaire dans `SessionForm.applySchedule`, qui nettoie aussi `repeat` ET `days` des membres et répare les anciens cycles « bidirectionnels »). À la lecture, `canonicalCycles()` (`src/lib/schedule.ts`) garantit qu'une séance n'appartient qu'à un seul cycle (premier propriétaire gagne) et les jours fixes des membres sont ignorés. `repeat: null` / `objective: null` / `goal: null` (et non undefined) pour désactiver — Firestore ne supprime pas les champs absents d'un update.
- Objectifs **à paliers avec récompenses** : `Exercise.goal.levels[]` (meilleure série/volume muscu) et `Session.objective.levels[]` (chaque palier = cibles multi-mesures à remplir dans une même séance + `reward`). Normalisés par `goalLevels()` / `objectiveLevels()` dans lib/metrics (gèrent les anciens formats `value`/`targets`). Gérés dans l'onglet `/goals` (création, modification ✎, suppression), célébrés à la complétion (CompleteSheet) **uniquement pour les paliers nouvellement franchis** (la récompense ne se débloque qu'une fois).
- **Sélecteurs partagés Aujourd'hui ↔ Planning** (`src/lib/planDay.ts` + `src/lib/planningLayout.ts` + `src/lib/usePlanAnchor.ts`, refactor « audit P1 » juin 2026) : la logique « séance du plan due / faite ce jour » et l'ordre des sections étaient dupliqués dans les deux pages et avaient divergé. Désormais source unique : `planWeekFor(date)` (semaine du plan alignée par lundi), `planWeekStates(week, logs)` / `planToDoOn(date, logs)` (état de chaque séance), et `planningSections(ordered, planActive, planAnchor)` (ordre vertical des sections + injection de la section « Running » du plan). **`done` corrigé** : une séance du plan est « faite » si un log porte son `planRef` (validation explicite) OU s'il existe une course **libre** (running SANS `planRef`) le jour prévu — on n'inclut PLUS « n'importe quelle course ce jour-là », sinon la validation d'une AUTRE séance déplacée sur ce jour faisait passer une séance non faite en « faite » (faux positif Planning) ou la masquait de la to-do (faux négatif Aujourd'hui). `usePlanAnchor(uid)` partage l'ancre de la section plan (lecture seule sur Aujourd'hui pour respecter l'ordre du Planning). **Perf** : `canonicalCycles(sessions)` mémoïsé une fois par page et passé en paramètre optionnel à `plannedSessionIdsOn` / `ownerOf` / `describeSchedule` (au lieu d'une reconstruction par jour ET par ligne). **Décomposition du conteneur Planning** (audit P2) : `usePlanningWeek(weekOffset, logs)` (`src/lib/usePlanningWeek.ts` : dates de la semaine, alignement de la semaine du plan, `planStates`, indice de démarrage) et `usePlanningOrdering({ sessions, planActive, planAnchor, savePlanAnchor, updateSession })` (`src/lib/usePlanningOrdering.ts` : ordre optimiste `orderIds`, `planningSections`, sensors + `handleDragEnd` sections/séances) ; `Planning.tsx` ne garde que la grille (cycles/plannedByDay/doneByDay/perWeek), `validateDay` et le rendu. **Identité des logs du plan** (audit P2) : `isPlanLog(l)` = `!!l.planRef` est LE discriminant (le `sessionId: ''` posé à la création dans WorkoutSheet est une convention à ne pas tester) ; `doneIds` (Aujourd'hui) et `doneByDay` (Planning) excluent les logs du plan, et le compteur d'en-tête « N séances/sem. » ajoute `planStates.length` (les courses du plan de la semaine, longtemps oubliées du total).
- L'écran Aujourd'hui navigue dans les dates passées (saisie rétroactive) **ET futures** (aperçu des jours à venir : libellé d'en-tête `isToday ? Aujourd'hui : isFuture ? À venir : Saisie passée`, état vide futur « journée repos ») : `CompleteSheet` accepte une prop `date`. **Le jour affiché est un OFFSET relatif** (`dayOffset`, et non une date figée au montage) → la vue suit le passage de minuit ; un écouteur `visibilitychange`/`focus` force le recalcul au retour sur l'onglet (audit P3). **Sur une date future, `CompleteSheet` permet la validation ANTICIPÉE** (août 2026, demande utilisateur — remplace l'aperçu seul de l'audit P3) : formulaire et saisie disponibles, bouton « Valider en avance ✓ », le log est journalisé à la date AFFICHÉE (future) ; seul le minuteur guidé reste réservé au jour J (`hasTimer` exige `logDate === todayStr()`). **Il injecte aussi les séances de course du plan semi** dues le jour affiché (via `planToDoOn`), en carte à-faire typée `TYPE_META` (icône `Footprints`) qui ouvre le `WorkoutSheet` (consulter + « Valider ma séance ») — exactement comme le Planning. Une séance déjà faite (cf. `done` ci-dessus) quitte la to-do (son log s'affiche dans « Terminées »). **L'ordre des cartes du jour suit EXACTEMENT le Planning** : on parcourt `planningSections(sessions, …)` et, dans la section « Running », on insère la séance du plan avant les séances utilisateur de la section (avant, Aujourd'hui listait un plat `sortOrder` ignorant les sections → ordre différent du Planning, bug juin 2026).
- **Échauffement automatique** (`Session.warmupFor`, août 2026) : une séance peut désigner une catégorie déclencheuse (mode « Avant une autre » du Seg de Planification de `SessionForm` — sept. 2026, auparavant enfoui dans « Options avancées » où l'utilisateur ne le trouvait pas) — elle s'invite alors en tête de la to-do d'Aujourd'hui les jours où une séance de cette catégorie est à faire (course du plan semi via `planToDo`, ou séance utilisateur planifiée), et disparaît une fois journalisée ou quand la déclencheuse est faite. Sélecteur pur `warmupsDueOn` (`src/lib/planDay.ts`), indépendant de `days`/`repeat` ; la séance n'apparaît PAS dans la grille du Planning (pas de jour propre). Cas d'usage d'origine : la routine « Échauffement course » (étirements) avant chaque course.
- Sémantique par sport : running = simple coche ; vélo = formulaire perfs (préremplies depuis le dernier log, programme prévu affiché à l'avance via `MetricDef.target`) ; muscu = séries×reps (exercices `measure: 'sec'` pour le gainage), avec **séries variées** possibles (`SessionItem.targets: number[]`, ex. 30/20/15, normalisé par `setTargetsOf()` dans types.ts qui prime sur `target`) ; HIIT/étirements/muscu = minuteur guidé `src/pages/Player.tsx` (WebAudio + vibration + wake lock ; en muscu les séries en reps s'ajustent en − / + puis « Série faite ✓ », le gainage et les repos sont chronométrés). Le Player journalise le **réalisé réel** (`doneRef`, muscu ET HIIT) : reps ajustées, durée cible si chrono tenu, temps écoulé si « Passer » ≥ 3 s ; séries « **mal réalisées** » marquables pendant la séance — bouton « Série ratée ? » sur l'écran de repos (dernière série, `lastSetRef`) et tap sur une ligne travaillée du tiroir programme (bascule toutes les séries enregistrées de l'exo, ⚠ ambre). Commandes du Player : ✕ = quitter SANS enregistrer ; ■ = arrêter ici (confirm + prompt commentaire, log partiel, note « Séance interrompue » par défaut) ; **touche espace** = pause/reprise (démarrage sur l'écran prêt), label « En pause » + anneau en pulsation ; **mode immersif sombre** (fond abysse `#04121b`, teinte bleutée `#08202e` au repos, accents par catégorie via `ACCENT` — désormais dérivé de `CATEGORY_META[].hex`, la charte bord de mer étant déjà claire — et `CALM` lagon au repos) : barre de progression globale sous le header, **anneau de progression** central (`Ring`, fraction restante `frac` rafraîchie ~10×/s ; piste seule pour les reps manuelles, − / + autour de l'anneau), **écran de repos dédié** avec carte « Prochaine série » (nom · série · objectif), commandes ■ / pause (blanche) / ⏭ regroupées au-dessus du **tiroir programme** : résumé permanent « Bloc 2/3 · Tour 1/2 » + « Ensuite : X », liste complète blocs/tours/exos dépliée au tap (`aria-label="Programme"`, ✓ faits / › courant accentué, prescriptions `itemLabel` à droite, auto-scroll, position via `Step.block/round/item`) ; détail central limité à « Série s/n » + pilule « démo » si l'exercice courant a une videoUrl. **CompleteSheet (Aujourd'hui) = consulter / lancer / entrer le résultat uniquement** : programme muscu en lecture seule structuré par blocs/tours (« Bloc 2 · × 2 tours », résumé « 3 × 20 reps » ou « 30 / 20 / 15 reps ») ; « Entrer le résultat ✓ » (muscu **et HIIT**) ouvre la **timeline à curseur** : toutes les séries dans l'ordre réel d'exécution, groupées par bloc/tour (en-têtes « Bloc b · Tour r/n »), tap sur une ligne = « je me suis arrêté ici » (elle et toutes les suivantes passent en non faites, exclues du log ; re-tap sur une non-faite la refait et requalifie ce qui précède), icône ⚠ par ligne = « **mal réalisée** » (`ExerciseResult.flagged` = indices dans `sets` ; annotation pure, la valeur compte quand même dans volume/paliers, affichée dans `logSummary`), sans steppers ni ajout/retrait de série — les valeurs journalisées sont les cibles. Le bouton de complétion s'appelle « Valider la séance ✓ » (+ « Fermer sans valider »).
- **Progression automatique des objectifs** (`src/lib/progression.ts`, juillet 2026) : hors course à pied, **la perf de la dernière séance devient la nouvelle cible**. Règle « **ne jamais reculer** » : cible = `max(cible d'origine, perf réalisée)` **série par série** — une séance interrompue ou moins bonne ne fait jamais baisser l'objectif, et les séries jamais faites sont ignorées. Sur un circuit multi-tours, on retient la **plus basse** des valeurs réalisées (la cible doit rester tenable à chaque tour : 12 · 12 · 8 ne monte pas la cible à 12). Couvre la muscu (cibles par série, reps et secondes), les étirements (`durationSec` / `target`), le HIIT (`durationSec` de l'intervalle) et les mesures saisies à la fin (vélo : durée, distance, puissance… — **`bpm` exclu**, une FC plus haute n'est pas un progrès). **Rien n'est écrit en base** (choix utilisateur) : `progressedSession(session, exercises, logs, upTo)` renvoie une séance **dérivée** + un flag `raised`, consommée par `CompleteSheet` (programme, timeline de saisie, bandeau « Objectifs relevés à hauteur de ta dernière perf ») et par le `Player` — jamais par `updateSession`. Le log ne gardant que le compte de séries **par exercice**, les perfs sont redistribuées sur les créneaux prévus (`plannedSlots`, même principe que `stopPoint`). Dans le Player la séance progressée est **figée au montage** (`frozenRef`) : le log écrit en fin de séance ne doit pas réécrire le programme en cours d'exécution. Pas d'emballement possible : valider depuis `CompleteSheet` journalise exactement les cibles, seul le Player (reps ajustées − / +) peut donc faire monter un objectif.
- **Bloc « Dernière fois »** (`CompleteSheet`) : en muscu/HIIT, la ligne principale dit **où la séance s'est arrêtée dans le circuit** — « Arrêté à Bloc 2 · Tour 2/3 — Squats · Série 1 · 9/15 séries » (ou « Séance complète · 15 séries ») — et le **détail par exercice passe en seconde ligne, entier** (avant : les 3 premiers exercices puis « … », donc l'info d'arrêt manquait, retour utilisateur juillet 2026). Le log ne mémorisant que le compte de séries **par exercice**, `stopPoint()` (`src/lib/timeline.ts`) rejoue la timeline prévue (`buildTimeline`) en consommant ces séries dans l'ordre réel et retient la **dernière consommée** — prendre la dernière plutôt que le premier manque reste juste quand une série a été sautée en cours de route (le Player n'enregistre pas les séries passées). Si plus rien ne correspond (séance modifiée/vidée depuis), `lastPerfLine` retombe sur `lastDetailLine` (détail complet, sans troncature).
- **Fiche de séance terminée** (`LogSheet`, ouverte en tapant une ligne « Terminées » d'Aujourd'hui — plus de lien « annuler » inline) : réalisé en **timeline éditable** préremplie depuis le log (valeurs réelles conservées ; log sans résultats « marqué faite » = tout ✓ ; séance supprimée → timeline reconstruite du log via `timelineFromLog`), **comparatif léger** « dernière fois : N · date » sous la 1re série de chaque exo, mesures du log et note éditables, « Supprimer cette séance » en bas (confirm, remplace l'ancien « annuler »). Persisté via `updateLog` (DataContext → `store.update('logs')`). La timeline est **partagée** : `src/lib/timeline.ts` (`buildTimeline`/`timelineFromLog`/`collectSets`, types `SetRow`/`SetStatus`) + `src/components/ResultTimeline.tsx` (UI tap/⚠, ligne `sub` optionnelle) — utilisée par CompleteSheet ET LogSheet.
- **Progrès — SUPPRIMÉ** (août 2026, demande utilisateur) : l'onglet, la route `/progress` et la page `Progress.tsx` (« Mes suivis », « Réussite des séances », fiches `ExoSheet`/`MetricsSheet`) ont été retirés, et **recharts désinstallé** avec eux (il n'avait pas d'autre consommateur). Les objectifs à paliers restent gérés dans `/goals` (route conservée, onglet masqué) et célébrés dans CompleteSheet.
- **Blocs** (`src/lib/blocks.ts`, muscu ET étirements) : une séance se découpe en blocs d'exercices répétés indépendamment (`SessionItem.blockBreak` démarre un bloc, `blockRounds` = ses tours, posés sur le 1er exercice du bloc). Sans découpage, un seul bloc avec `Session.rounds` (« Tours du circuit » muscu / « Tours de la routine » étirements). Utilisé par CompleteSheet (programme et saisie par tours) et le Player ; le superset reste muscu-only. `stretchTotalSec` (format.ts) compte blocs et tours. Dans la fiche séance, **les blocs entiers se déplacent au drag & drop** par la poignée de leur en-tête (ids `blk-{uid}` dans un SortableContext de blocs, items dans un SortableContext par bloc, un seul DndContext ; déplacer la tête d'un bloc transfère le bloc à l'exercice suivant).
- **Étirements : `Exercise.measure` est respectée** — posture tenue (`sec`, défaut du seed/quickCreate/ExerciseForm pour cette catégorie) = `durationSec` chronométré ; mouvement compté (`reps`) = `SessionItem.target`, étape manuelle « Fait ✓ » dans le Player et « N reps » dans le programme. Bascule sec/reps dans la ligne d'item (modifie l'exercice globalement). Les anciens exercices d'étirements créés en `reps` par défaut s'affichent désormais en répétitions : un tap sur la bascule les corrige.
- Planning : **vue semaine par semaine avec dates** (navigation par flèches, état `weekOffset` ; `monday = startOfWeek(today) + weekOffset*7` ; en-tête de jours = lettre + numéro du jour, « aujourd'hui » surligné uniquement sur la semaine en cours `weekOffset===0`). Sections personnalisées via `Session.group` (champ « Section du planning » de la fiche, drag & drop d'une séance entre sections) ; ronds de la semaine affichée = anneau « prévu » / plein « fait » ; **toucher un rond valide/dévalide la séance ce jour-là (crée/supprime un log)** — la grille ne modifie jamais la planification, qui s'édite dans la fiche. **Une séance non planifiée (ni jours fixes, ni rotation, ni membre d'un cycle) n'apparaît PAS dans la grille** — sauf les semaines où un log la marque faite (le rond plein reste) ; son pointage passe par « Aujourd'hui »/Séance libre (sept. 2026, demande utilisateur). **Réordonnancement des sections entières par drag & drop de leur poignée d'en-tête** (ids `sec-{group}`, `SortableSection` ≈ pattern blocs muscu : un seul `DndContext`, `SortableContext` externe des en-têtes + un `SortableContext` interne par section pour les séances ; `handleDragEnd` branche sur le préfixe `sec-`). L'ordre des sections **utilisateur** dérive du `sortOrder` (plus bas par groupe), donc les déplacer réécrit le `sortOrder` de toutes les séances par blocs de section via `persistOrder` (poignées dès ≥ 2 sections déplaçables). **La section « Running » du plan est elle aussi déplaçable** même sans séance à elle : comme elle n'a pas de `sortOrder` à porter, sa position est mémorisée par une **ancre** (`planAnchor` via `usePlanAnchor`, localStorage `elan-plan-anchor-{uid|local}` : clé de la section utilisateur juste en dessous d'elle, ou `'__end__'`/`'__start__'`) et la section est insérée à cette position par `planningSections` (au lieu d'être figée en tête). Un drag réécrit le `sortOrder` des sections utilisateur ET met à jour l'ancre du plan (`savePlanAnchor`). Cas `hasRunningGroup` (l'utilisateur a déjà un groupe « Running ») : le plan s'injecte dans cette vraie section, ordonnée normalement par `sortOrder`, sans ancre. **Séances du plan dans la section « Running »** : `planWeek` = la semaine du plan dont le lundi (`start`) **correspond à la semaine affichée** (`planWeekFor(monday)`, `src/lib/planDay.ts`) → les séances tombent sur leurs **vraies dates** ; avant le départ du plan, rien ne s'affiche sur la semaine en cours (plus d'illusion de « retard »), et un indice « Le plan semi démarre {date} → » saute à la 1re semaine (`planStartOffset`). Elles s'injectent **dans la section `Session.group` « Running » de l'utilisateur s'il en a une** (en tête, au-dessus de ses propres séances Running) ; sinon une section « Running » synthétique est insérée à sa position d'ancrage (voir `planAnchor` ci-dessus). Elles sont en **lecture seule**, via `PlanRow`, sur leur colonne de jour (`PlanSeance.day`), **typées par couleur/libellé** (`TYPE_META` dans `plan.ts` : Footing/EF vert, Sortie longue bleu, Seuil ambre, Allure semi orange, Fractionné corail, Course violet) pour pouvoir organiser ses HIIT/muscu autour. Le rond **plein « fait » se pose sur le jour RÉELLEMENT effectué** (date du log validant, qui peut différer du jour prévu — ex. séance du vendredi faite le jeudi), une fois la séance faite, le jour prévu ne porte PLUS d'anneau (le rond plein du vrai jour suffit ; un anneau vide ailleurs se lisait à tort comme « en attente » — retiré juin 2026 sur retour utilisateur). L'anneau « prévu » ne s'affiche donc que tant que la séance n'est pas faite (`showPlannedRing = d === s.day && !done`) ; `doneCol` dérivé de `planWeekStates(planWeek, logs)` (`PlanSeanceState.doneDate`, source partagée — cf. sélecteurs partagés ci-dessus). Taper une séance du plan ouvre le `WorkoutSheet` (fiche Campus + « Valider ma séance »). Le même `TYPE_META` colore la tuile et ajoute le libellé du type sur les cartes de l'onglet Plan.
- **Formulaires épurés** (demande utilisateur, juin 2026) : les fiches séance/exercice n'affichent plus de murs de chips — catégorie en `Select`, section du planning et sous-types via `Combobox` (`src/components/ui.tsx` : saisie → filtre (label OU groupe) → « + Créer » en fin de liste si aucune correspondance exacte ; en-têtes de groupes via `option.group` en bandeau sage, panneau en z-50 au-dessus de la barre d'action). Les exercices d'une séance s'ajoutent par **`ExercisePicker`** (`src/components/ExercisePicker.tsx`, août 2026 — remplace la combobox qui se refermait à chaque sélection) : recherche + groupes de sous-types avec duplication multi-tags comme la banque, tap = ajout immédiat sans fermer, coche ×n sur les exercices déjà dans la séance, « + Créer » = mini-ligne nom + sous-type + mesure (l'exercice naît classé, `appendItem(exId, measure)` ne dépend pas de l'abonnement store pour un exercice tout juste créé). Il s'affiche en **volet sticky permanent à droite sur desktop ≥ lg** (la fiche séance passe en deux colonnes, `App.tsx` élargit `main` à 62rem sur `/session/`) et en **Sheet plein écran sur mobile** (ligne « + Ajouter un exercice » en pied de liste, bouton « Terminé » ; sur desktop la même ligne envoie le focus dans la recherche du volet via `searchRef`) — ⚠ les DEUX instances vivent dans le DOM en même temps, les sélecteurs Playwright doivent viser `[role="dialog"]` ou `aside`. Le formulaire de séance n'édite plus `metrics`/`links`/`notes` : ils sont **préservés tels quels** à la sauvegarde (le vélo retombe sur `DEFAULT_VELO_METRICS` via `effectiveMetrics`) et toujours affichés dans CompleteSheet s'ils existent. Boutons Enregistrer/Dupliquer/Supprimer regroupés dans `FormActions` (barre fixe en bas, poubelle en teinte danger) ; la BottomNav est masquée sur `/session/*` et `/exercise/*` (`formScreen` dans App.tsx).
- **Fiche séance refondue** (sept. 2026, audit UX + maquette « Fiche séance » sur canvas Claude Design, direction B « listes groupées » choisie par l'utilisateur sur trois visuels) : l'écran avait été assemblé sans maquette (la charte n'a aucun écran de formulaire) — deux chartes mélangées, six registres typographiques, phrases d'aide en Nunito gris, popup de `<select>` illisible sous Windows. Nouveau vocabulaire, local à `SessionForm.tsx` (`card`/`row`/`rowLabel`/`iconBtn*`/`miniInput`) : **une carte de verre par section**, rangées de 48 px « libellé mono à gauche, valeur à droite » avec `border-t` porté par chaque rangée (pas de `divide-y`, le drag & drop intercale des enveloppes). Ordre : nom + **tuiles de catégorie** (codes `RUN/VÉLO/MUS/HIIT/ÉTIR`, teinte `CATEGORY_META.hex`) → **exercices** (en-tête = compte + réglages de la séance : Tours en `Stepper` carré, effort/repos HIIT et transition étirements en `MiniNum` ; lignes repliées = poignée · nom · commentaire en sous-ligne · résumé mono `3 × 20 · 60 S` · chevron ; **ligne dépliée** = séries/cible/repos + champ commentaire + description + **actions en icônes** (`SlidersHorizontal` varier, `MessageSquarePlus` commenter, `Play` démo, `FileText` fiche, `Trash2` retirer) ; blocs = en-tête `Bloc n` teinté avec ses tours et `Merge` pour fusionner ; **superset = pastille sur le filet** entre deux lignes, `aria-pressed` ; pied « + Ajouter un exercice · nouveau bloc ») → **planification** (voir le point « Planification » plus haut : `Seg compact` à trois positions Jours fixes / Tous les X jours / Avant une autre, cases de jour 36 px `DayButton` sur toute la largeur de la rangée sans libellé, ligne « En alternance avec » (sélecteur « Aucune » → pastille avec sa croix), « Commencer par » avec les noms, aperçu = grille du Planning semaine par semaine ; « Avant une autre » = `warmupFor` en `Chip` de catégories sous le libellé « Avant chaque séance de » — libellé au-dessus, chips en dessous, sinon elles passaient sur deux lignes et chevauchaient le libellé — présélectionné au basculement, sans section d'alternance) → **options avancées repliées** derrière leur résumé (le nom de la section, ex. « Routine matinale ») : section du planning (`Combobox`) seule. **Aucune phrase d'aide** (décision utilisateur). **Garde-fou du retour** : instantané JSON du formulaire au montage, `confirm('Abandonner les modifications ?')` si différent — les scripts Playwright doivent accepter les dialogues. **Brouillon de navigation** (06/09/2026, bug utilisateur : ouvrir la fiche d'un exercice depuis la séance démontait le formulaire et perdait tout) : le même objet `Draft` (tout l'état éditable) est mis de côté en `sessionStorage` (`elan-session-draft-{id|new}`, horodaté) par `openExerciseSheet` juste avant `navigate('/exercise/…')`, et `takeDraft` le consomme au montage suivant de la même fiche (une seule restauration, ignoré passé une heure) pour initialiser les `useState` ; une fiche restaurée pose `initialRef = ''` pour que le retour demande confirmation. Seule cette navigation met le brouillon de côté : « Retour » abandonné ou fiche enregistrée n'en laissent aucun. Plus de `Fab` sur mobile (une seule affordance d'ajout). Primitives touchées dans `ui.tsx` : `Seg` gagne `compact` (34 px, 9 px sous `sm`) et `short` (libellé abrégé mobile, `aria-label` = nom complet, ce qui garde `getByRole({ name: 'Tous les X jours' })`), `Stepper` passe en carrés à filet (il n'a plus d'autre consommateur), `FormActions` teinte la poubelle. `ExercisePicker` : noms en Nunito 15 comme la liste (plus de display), icône de recherche, `searchRef`. `index.css` : `select option { background: shoal; color: ink }` corrige le popup natif (le `<select>` « Choisir une séance… » de la rotation et celui d'`ExerciseForm` restent natifs). Scripts adaptés : `check-form-ui` (bouton « Ajouter un exercice », `a[aria-label="Démo"]`, clic « Options avancées » avant la combobox, `[title="Tours de la routine"]`, « Bloc 2 »), `check-blocs` (« Bloc 1/2 »), `check-v4` (`[title="Tours du circuit"]`, `button[aria-pressed="true"]:has-text("superset")`), `check-weekday-cycle` (Jours choisis + partenaire = cycle sur jours de semaine). Ancres conservées exprès : `button[title="Lundi"]`, `select[aria-label="En alternance avec"]` + `[aria-label="Retirer l'alternance"]` (alternance simplifiée du 05/09 au soir — remplacent `select:has-text("Choisir une séance")`, le bouton « Ajouter » et `[aria-label="Ajouter une séance au cran A"]`), `span:text-is("Avant chaque séance de")` (rangée des chips de `check-warmup-ui`), `[aria-label="Varier les séries"]`, `[aria-label^="Réordonner"]`, `div.rounded-md p.truncate` (premier `p.truncate` = premier exercice), « Exercices de la séance ».
- **Charte minimaliste** (juin 2026) : icônes **lucide-react** partout — y compris les catégories via `CategoryIcon` (`ui.tsx`, couleur héritée du parent ; `CATEGORY_META` n'a **plus de champ `emoji`**, et les `<option>` natives affichent le label seul). Un seul style de conteneur (carte `rounded-2xl shadow-sm`, un seul niveau d'inset sage-50 max), pas de bordures pointillées. Émojis conservés uniquement pour le contenu émotionnel (🎉 🎁, états vides). Planification intervalle = une ligne « Tous les [n] jours, à partir du [date] » ; rotation = lignes « J1/J2 » compactes dont le sélecteur n'apparaît qu'au tap sur « + » (state `addingDay`), sans encadré récap ; « Tours du circuit » promu en carte pleine ligne.
- **Charte « bord de mer », étape 1 — les couleurs** (juillet 2026) : l'app passe du **clair vert sauge/crème** au **sombre ardoise/écume**. Ce premier basculement s'est fait **par les tokens, pas par les écrans** — les composants étaient déjà écrits en classes sémantiques (`bg-surface`, `text-ink`, `bg-sage-100 text-sage-700`), donc **seules les valeurs de `@theme` dans `src/index.css` ont changé**, en gardant les noms. ⚠ **Attention au piège de provenance** : cette étape a été menée à partir du seul fichier alors disponible, `Élan sport en bord de mer.zip`, qui **ne contient QUE des icônes** (`assets/icons/` + un README de géométrie du logo + le manifest). Il ne dit RIEN de la typographie, des composants ni de la mise en page : la palette sombre en a été *déduite* des deux couleurs du logo. La vraie maquette est arrivée après — voir l'étape 2. Points à connaître avant de toucher une couleur :
  - **La rampe `sage-*` est INVERSÉE.** En clair elle allait du fond pâle (50) au texte foncé (700) ; en sombre elle va du fond profond `#0f2a38` (50) au texte lumineux `#b9e9ef` (700). C'est ce qui rend `bg-sage-100 text-sage-700` juste dans les deux mondes — ne pas la « remettre à l'endroit ».
  - **Empilement des fonds** : page `cream #061722` → carte `surface #0e2634` → inset `sage-50` → **élément posé sur un inset ou champ de saisie `shoal #123449`**. Un enfant qui reprend le fond de son parent (le classique `bg-surface` dans une `Sheet`, elle-même `bg-surface`) **disparaît** : c'est LE piège du thème sombre, et la raison d'être de `shoal` et de `check-charte.mjs`.
  - **`text-onaccent` (`#04121b`) remplace `text-white` sur les aplats d'accent** : l'accent lagon est clair, du blanc dessus serait illisible. `text-white` ne reste légitime que dans `Player.tsx` (écran immersif, fond abysse).
  - **Ombres redéfinies** dans `@theme` (`--shadow-sm`…`--shadow-2xl`, noirs profonds de la marque) : à 5 % d'opacité elles ne se voyaient plus.
  - **`color-scheme: dark` sur `html`** : sans lui, les contrôles natifs (`<select>`, `<input type="date|time">`, barres de défilement) restent blancs.
  - **Les hex en dur sont des miroirs de tokens, pas des couleurs libres** : `CATEGORY_META[].hex` sert aux aplats inline et aux graphes, `TYPE_META`/`PLAN_ZONES` (`plan.ts`) et `KIND` (`WorkoutSheet.tsx`) colorent les séances du plan. Changer un token de couleur impose de mettre à jour son miroir.
  - Catégories et types d'effort ont été **remontés en clarté** : les teintes sourdes de la charte claire disparaissaient sur l'ardoise. Valeurs actuelles (étape 2) : running `#f09a72`, vélo `#7ec8e3`, muscu `#9faeda`, HIIT `#f07b68`, étirements `#6bd0c2`.
- **Charte « bord de mer », étape 2 — la maquette** (juillet 2026) : source de vérité = **`Élan - Bord de mer (hors ligne).html`** à la racine du dossier SPORT (artefact bundlé : le code lisible est dans le `<script type="__bundler/template">`, les binaires — polices et photo — dans le `manifest`). Elle décrit **7 écrans** : Jour, Semaine, Exercices, Cap (objectifs), Sillage (progrès), En mer (minuteur), Journal de bord. Elle porte aussi un système générique « Saintilann » (palettes ember/azure/jade, `--bg: #0C1E3E`) qui **n'est PAS ce qu'utilisent les écrans** : ceux-ci appliquent une variante bord de mer en styles inline sur `#071A26`. **Suivre les écrans, pas ce bloc de tokens.** Trois matières indissociables :
  - **la PHOTO** de bord de mer (le phare d'Ar-Men) en fond de TOUTE l'app — `src/components/SeaBackdrop.tsx`, monté une fois dans `App` en `fixed -z-10`, hors Player. Le master JPEG vit hors app (racine du dossier SPORT, comme le doc du plan) ; `scripts/make-photo.mjs` le recadre et l'encode en WebP (780×1560, ~167 ko) dans `src/assets/`. Le `brightness-[0.62] saturate-[0.78]` sur l'image **n'est pas cosmétique** : le master est une image de vente très contrastée, dont l'écume remontait par-dessus le scrim jusqu'à concurrencer le texte ;
  - **le VERRE DÉPOLI** : les cartes deviennent translucides (`bg-glass`, `bg-glass-soft`, `bg-glass-sunken`) avec un filet `border-hairline`. Le `backdrop-blur` **n'est pas optionnel** — sans lui la photo traverse en clair et le texte devient illisible. Corollaire : `bg-surface` (opaque) n'est plus la carte par défaut, il ne reste que sur les écrans pas encore reprises ;
  - **DEUX TYPOGRAPHIES** qui portent tout le style : `font-display` (**Big Shoulders Display**, condensée, 700/900, **toujours** en `uppercase`) pour les titres, `font-mono` (**Space Mono**, 9–11 px, `tracking-[0.12em]`…`[0.22em]`, capitales) pour **toute** métadonnée. Nunito ne sert plus qu'aux rares paragraphes explicatifs. Les rayons deviennent quasi carrés : carte `rounded-md` (6 px), bouton `rounded-sm`, tuile `rounded-xs`, pilule `rounded-full`.
  - Primitives partagées dans `ui.tsx` : `Eyebrow` (sur-titre mono), `DisplayTitle`, `Pill` (compteurs), `CodeTile` (le carré `ÉTIR`/`VÉLO`/`SEUIL` qui remplace l'icône Lucide sur les cartes ; alimenté par `CATEGORY_META[].code` et `TYPE_META[].code`, et il **réduit sa taille au-delà de 4 signes**), plus la classe `glassCard`.
  - **État d'avancement : toute l'app est passée à la charte.** Repris écran par écran : **Aujourd'hui** (`Today.tsx`), **Planning** (`Planning.tsx`), **Exercices** (`Library.tsx`), `BottomNav`.
  - **L'onglet s'appelle « Exercices »** depuis le 03/09/2026 (`BottomNav.tsx`, décision utilisateur ; il s'était appelé « Séries » d'août à septembre 2026). L'écran (`Library.tsx`, titre « Exercices », sur-titre « — Mes programmes ») est **la liste des programmes** de l'utilisateur, refondue le même jour sur la direction « aperçu déplié » choisie sur maquette (canvas « Écran Exercices ») parce que l'ancienne liste « ne paraissait pas éditable » : **une carte de verre par programme, la même que sur Aujourd'hui** (tuile de sport, nom, résumé `summarizeSession`, planification `describeSchedule` en ligne avec icône calendrier/↻ — plus de pilule, elle ressemblait à un bouton), **plus de section par sport** (la tuile le porte), **un tap déplie la carte SUR PLACE** — programme en lecture seule via `ProgramView` (exporté de `CompleteSheet.tsx`, partagé avec la feuille d'Aujourd'hui) + bouton « Modifier » vers la fiche —, **un seul programme déplié à la fois** (`openId`), chevron carré tourné vers le bas, carte en `glass-raised`/`hairline-strong`. Desktop ≥ lg : `main` élargi à 62rem (`wideScreen` dans `App.tsx`, comme la fiche séance) et cartes sur **deux colonnes** (`items-start`). La banque reste un bouton « Banque d'exercices › » dans l'en-tête, à droite du sur-titre. Le FAB dit « + Programme » ; `describeSchedule` renvoie « Non planifié » (masculin, pour « programme »). ⚠ **« Séries » garde son sens de répétitions** ailleurs (« Série faite ✓ », « 3 × 12 reps », « Varier les séries », `ExerciseResult.sets`). Scripts : 8 ciblent l'onglet par `getByRole('link', { name: 'Exercices', exact: true })` — **`exact` obligatoire**, et jamais `text=Exercices` (matche « Exercices de la séance » et « Banque d'exercices ») ; attendre l'écran par `text=Mes programmes` ; ouvrir une fiche = clic sur le titre `p:has-text("…")` PUIS `getByRole('button', { name: 'Modifier', exact: true })`. La ligne de planification d'une carte est un `<div>` (pas un `<p>`) exprès : elle cite d'autres séances (« en alternance avec HIIT — Cardio express ») et `p:has-text` attraperait la mauvaise carte.
  - **La banque d'exercices est un SOUS-ÉCRAN, plus un onglet à parts égales** (août 2026, demande utilisateur) : `Library.tsx` ne montre que la liste des programmes et un bouton « Banque d'exercices › » dans son en-tête ; la banque vit dans `src/pages/ExerciseBank.tsx` sur la route `/library/exercices`, avec bouton Retour dans son `PageHeader` et la BottomNav conservée (c'est un écran de consultation, pas un formulaire). Motif : on n'y va qu'occasionnellement — corriger un nom, un sous-type, une mesure sec/reps, une URL de démo — la **création** d'exercice passant déjà par la combobox « + Créer » de la fiche séance. Le `Seg` moitié-moitié donnait à tort le même poids aux deux. Conséquences à connaître : `ExerciseForm` redirige la **suppression** vers `/library/exercices` (l'ancien `/library?tab=exos` retombait sur les séances, le paramètre `tab` n'existant plus), et `check-v4.mjs` sort de la banque par `[aria-label="Retour"]` au lieu du segment « Mes séances ». `smoke.mjs` et `check-form-ui.mjs` continuent de marcher tels quels : le lien porte le même nom accessible que l'ancien segment. Les autres ont basculé **par les primitives** de `ui.tsx` : `PageHeader` (sur-titre mono + titre display), `Sheet`, `Seg`, `Chip`, `Fab` (l'aplat corail `flare`), `PrimaryButton`/`GhostButton`, `EmptyState`, `FormActions`, champs de saisie et panneau de combobox. Trois passes mécaniques (scripts jetables, non conservés) ont fini le gros œuvre : **33** conteneurs opaques → verre dépoli, **17** sur-titres → Space Mono, **17** titres → Big Shoulders capitales. Le **Player** a reçu le traitement de l'écran « En mer » (chrono en display, sur-titre et détails en mono, boutons rectangulaires) et **LogSheet** le sur-titre « — MUSCU · 29.07 » du « Journal de bord ».
  - **`ui.tsx` est exclu des passes globales sur `text-* font-extrabold`** : ses `text-base/xl font-extrabold` habillent les glyphes − / + des steppers, pas des titres — les passer en display les déformerait.
  - Ce qui n'a **pas** été aligné sur la maquette, volontairement : la structure du Player (anneau de progression, tiroir programme, écran de repos) et la timeline à curseur de `LogSheet`/`CompleteSheet` — la maquette y montre des steppers − / +, mais l'app a fait le choix inverse en juin, sur demande utilisateur. Seul l'habillage a changé.
  - **Les vérifs Playwright s'accrochent parfois aux classes CSS.** `check-form-ui` ciblait `div.rounded-2xl.bg-surface` → devenu `div.rounded-md.bg-glass`. Si un sélecteur casse après un changement de charte, c'est presque toujours ça (ou un libellé renommé, cf. le point précédent).
  - **Le Planning doit tenir SANS DÉFILEMENT**, barre d'onglets comprise (demande utilisateur, juillet 2026) : la grille de la semaine se lit d'un coup d'œil ou elle ne sert à rien. D'où l'en-tête compact (pas de paragraphe explicatif, titre en 44 px et non 64), les lignes en `p-1` et les cellules de jour en 30 px. `node scripts/check-semaine.mjs` **mesure** ce débordement (`scrollHeight` vs `innerHeight`) et échoue s'il redevient positif — à relancer après toute addition dans cet écran.
  - **Grille du Planning** : colonnes `14px / 1fr / 7 × 28px` (la maquette dit 30, on récupère 14 px pour la colonne du nom, la plus étreinte). Le rond d'un jour est `DayDot` — anneau = prévu, plein = fait (avec le remplissage `tide`), point = rien ; la colonne du jour courant se signale par un fond, jamais par le rond. Le sous-libellé d'une ligne est `CODE · ↻ cadence` (ex. `MUS · ↻ TOUS LES 2 JOURS`) : **garder le `↻`**, `check-interval` le cherche tel quel.
  - **Deux libellés de la maquette n'ont PAS été repris**, volontairement : « + SÉANCE HORS PLANNING » (l'app garde « + Séance libre ») et « — AU JOURNAL » (l'app garde « Terminées », en `h2`). Motif : 8 scripts Playwright et ~25 sélecteurs ciblent ces textes, pour un gain purement éditorial. Le `uppercase` CSS donne de toute façon le rendu de la maquette. La navigation garde aussi ses 5 onglets actuels (la maquette propose JOUR/SEMAINE/EXOS/CAP/SILLAGE, sans l'onglet Plan « 21K » et avec Cap à la place) — un renommage d'onglets est une décision produit, pas une question de charte.
- **Plan semi** (`/plan`, onglet « Plan ») : plan d'entraînement statique `src/data/plan.ts` (PLAN_SEMI — 17 semaines du **14/06 au 04/10/2026** (S1 = semaine de reprise, une sortie longue 8 km le dim. 14/06 ; semaine ancrée au lundi 08/06), semi Tout Rennes Court, objectif 1h50, séances mar/mer/ven/dim, **allures calibrées sur les vraies données COROS** : EF 5:45–6:05 (pilotée par FC ≤150, recalibrée 13 juin sur les runs récents), seuil 5:20→5:08, AS 5:08–5:13, VMA 4:35–4:50 + zones FC). Phases **Fondation/Développement/Spécifique/Affûtage** (pic ~49 km S14). Chaque séance porte un **`Workout` structuré** (`WorkoutStep[]` + `RepeatBlock` pour les intervalles : `kind` warmup/work/recovery/cooldown/steady, durée OU distance, `Pace` fixe/fourchette, `hr`). Page **en vue semaine par semaine** (style Decathlon Coach) : navigation par flèches ← / → (`weekIdx`, init sur la semaine courante), carte « Ton aperçu hebdomadaire » (activités / durée / distance, calculées via `workoutStats`), puis une **carte par séance** (tuile icône, chip statut Aujourd'hui/Non faite/À venir selon un log `running` à sa date via `seanceDateStr`, « Séance n/4 · jour », titre, durée, distance, **difficulté en éclairs** via `TYPE_DIFFICULTY`), allures repères en pied ; **comme « Aujourd'hui », les séances VALIDÉES quittent le flux principal et passent dans une section « Terminées » compacte** (lignes `DoneSeanceRow` avec ✓, ouvrant la fiche pour consulter/dévalider) — la liste du haut ne montre que les séances restant à faire, avec un « Semaine bouclée 🎉 » quand tout est validé (split `todo`/`done` sur `isValidated` = `planRef` OU course ce jour-là, demande utilisateur juin 2026) ; **taper une séance ouvre `WorkoutSheet`** (`src/components/WorkoutSheet.tsx`) — affichage **type COROS Campus** : mini-diagramme à barres (répétitions développées, couleurs par `kind` : vert EF, orange travail, rose récup), liste des séquences (pastilles Échauffement/Récupération, badge ×N sur les blocs), résumé `≈ km · min`, tag route/piste (pas de bouton d'export in-app : l'export est un script local). **Export vers COROS — FAIT, via intervals.icu** (pont gratuit, partenaire COROS officiel — COROS n'importe PAS les .fit d'entraînement *planifiés*, seulement les activités terminées ; TrainingPeaks aurait marché aussi mais ses workouts structurés sont payants ~120 €/an). Le script `scripts/push-to-intervals.mjs` mappe chaque `Workout` vers le format texte intervals.icu (`- 20m 6:20-6:40/km`, `8x`, `- 0.4km 4:40/km`, récup `Z1 Pace`) et crée les événements `category:WORKOUT type:Run` ; intervals.icu (compte gratuit de l'utilisateur, connecté à COROS avec « Upload planned workouts » coché) synchronise la semaine à venir vers la montre. **`src/data/plan.ts` est LA source unique des séances** (août 2026) : l'app, le tableur `Plan-Semi-Rennes-2026.xlsx` (via `scripts/gen-xlsx.mjs`) et la montre (via `push-to-intervals.mjs`) en dérivent tous les trois. `gen-xlsx.mjs` vivait à la racine du dossier SPORT avec **sa propre copie des séances en dur** : le plan de l'app a été décalé d'une semaine le 10 août (semaine du 3–9 manquée, mal de dos), pas celui du tableur, et les deux ont divergé sans que rien ne le signale — d'où le déplacement du script dans `scripts/` et sa réécriture sur `PLAN_SEMI`. Le mémo `Plan-Semi-Rennes-2026.md` (le pourquoi) et `Memo-Conception-Plan-Semi-Rennes-2026.md` (la méthode) restent à la racine du dossier SPORT, hors app. **Rien ne redescend de COROS ni d'intervals.icu** : une séance modifiée directement dans la montre est invisible de l'app et sera écrasée au prochain `--clear --push`. Modifier `plan.ts`, puis republier l'app ET repousser vers la montre. **Validation manuelle d'une séance** (`WorkoutSheet`) : bouton « Valider ma séance » → sélecteur des **courses importées** (collection `activities`, écrite par la **synchro Strava in-app**, lue via DataContext comme `ideas` — non bloquante, dégrade en vide) **filtré aux sorties de moins d'une semaine** (`a.date >= aujourd'hui − 7 j`, puis 12 max) — au-delà ce n'est pas la course à valider — **ET excluant les sorties déjà utilisées** pour valider une autre séance (`usedIds` = `activityId` des logs existants ; une course = une séance, demande utilisateur juin 2026) ; on choisit la vraie sortie (ou « Valider sans associer ») → **étape de confirmation de date** (state `step: idle|picking|confirm`, `chosen`, `date`) pré-remplie avec la date de la sortie COROS (ou le jour prévu), **éditable** → crée un `Log` running avec `planRef` = `elan-{date}` (+ `activityId`, métriques distance/durée) à la **date confirmée** (le panneau « validée » affiche « Validée le {date} »). La validation est **découplée de la date** : la page Plan marque « Validée » si un log porte ce `planRef` (sinon repli sur l'ancienne coche par date via `runDates`) ; le Planning affiche le rond « fait » sur la date réelle (cf. `doneCol`). Permet de courir une séance en avance/retard et de la pointer soi-même, façon Campus. **Date d'une séance terminée éditable** (`LogSheet`) : champ date « Faite le » (state `logDate`, écrit dans `updateLog({ date })`) — vaut pour toutes les disciplines, pour corriger le jour d'une validation.
- **Plan vélo d'appartement** (`src/data/planVelo.ts`, août 2026) : endurance de base sur **12 semaines (24/08 → 15/11/2026)**, calibré sur les vraies séances de l'utilisateur (45 min · résistance 10) et sur son **organisation réelle** : les dates du plan sont les jours VÉLO de son alternance vélo/HIIT lun/jeu/sam (cycle `repeat.onDays` de « Vélo d'appartement ») — semaine à 2 séances (lun+sam) puis à 1 (jeu), en boucle. Progression : la durée d'abord (45 → 60 min), la résistance ensuite (10 → 11) ; semaines du 21/09 au 11/10 **légères exprès** (affûtage, semi le 04/10, récup). ⚠ Si le cycle lun/jeu/sam change dans l'app, régénérer les dates du plan (elles sont figées en dur, comme PLAN_SEMI). **Intégration volontairement légère** : le plan ne crée AUCUNE séance dans Aujourd'hui/Planning/rappels (l'alternance utilisateur les planifie déjà) — il vit dans l'onglet Plan (`Seg` « Course · 21K / Vélo » en tête de `Plan.tsx`, l'onglet garde le nom « 21K » que 2 scripts ciblent) avec vue semaine, cartes (durée/résistance/FC guide) et **validation manuelle** (`VeloSheet` : confirmer la date → log vélo `planRef: 'elan-velo-{date}'` + mesures durée/résistance) ; et **CompleteSheet surcharge les cibles vélo** (`veloSeanceOn(logDate)` remplace les targets `duration`/`power`) les jours du plan, si bien que valider la séance vélo normalement suffit à pointer le plan (« done » = planRef OU log vélo libre le jour prévu, même règle que le semi). Vérif : `check-plan-velo.mjs` (horloge figée au 29/08/2026).
- Tailwind v4 : thème en CSS-first dans `src/index.css` (`@theme`), pas de tailwind.config. **C'est le fichier unique de la charte** : couleurs, ombres et police y vivent, et un token y suffit à repeindre toute l'app (cf. « Charte bord de mer » ci-dessus).

- **Synchro Strava in-app** (`useStravaSync` + `DataContext.syncStrava`) : bouton **« Synchroniser »** (sélecteur de validation `WorkoutSheet` + section « Courses Strava » des Réglages) qui importe les vraies courses sans intervals.icu ni site. Chaîne : **COROS → Strava** (push auto natif, ~minutes) **→ Worker Cloudflare** (`worker/`, détient le secret, rafraîchit le token côté serveur car Strava bloque le CORS sur `/oauth/token`, renvoie les runs au format `Activity`) **→ app** (fetch du Worker via `VITE_STRAVA_SYNC_URL`, dédoublonnage par `externalId` préfixé `strava-`, écriture dans `users/{uid}/activities` — les règles Firestore autorisent l'utilisateur connecté à écrire). Le bouton n'apparaît que si `VITE_STRAVA_SYNC_URL` est défini (`stravaSyncConfigured`), donc déployable sans risque avant le setup utilisateur. Dernière synchro mémorisée en localStorage `elan-strava-last`. Worker déployé : `strava-elan.saintilan.workers.dev`. Le pont **intervals.icu** pour la récupération des courses (`pull-activities.mjs`) a été **supprimé** au profit de Strava (juin 2026) ; intervals.icu ne sert plus QUE pour le **push du plan → montre** (`push-to-intervals.mjs`). Décision : un bouton 100 % sans backend est impossible (token Strava expire à 6 h, refresh CORS-bloqué) → le Worker est le mini-relais minimal.

- **Rappels de séance par notification push** (juillet 2026) : notification « Séance du jour — Fractionné 6×800m · Gainage » à l'heure choisie (7h30 par défaut), rien les jours de repos ni si la séance est déjà validée ; tap → ouvre l'app. **Jusqu'à 4 rappels par jour** (`MAX_HOURS`, miroir app/Worker) : `entry.hours: string[]` trié, et `entry.lastSent` est un objet **par heure** (`{ '07:30': '2026-07-10' }`) — c'est ce qui laisse partir le rappel de midi alors que celui du matin est déjà parti. Le titre dépend du **rang** de l'heure dans la liste triée (`notificationText(names, rank)`) : rang 0 = « Séance du jour » (annonce), au-delà = « Pas encore fait » (relance) ; un rappel unique posé le soir a donc le rang 0 et annonce, ce qui est correct. Une relance de midi/soir se tait d'elle-même si la séance a été validée entre-temps, puisque l'app réécrit l'agenda à la validation et que le jour en disparaît. Deux heures dont les fenêtres de 15 min se chevauchent → le Worker n'envoie que la plus tardive et marque l'autre (`alsoMark`), jamais deux notifications d'affilée ; l'app refuse de toute façon deux rappels à moins de `MIN_GAP_MIN` (15 min). Rétrocompatibilité : l'ancien champ `hour` (heure unique) et l'ancien `lastSent` (une date) sont normalisés par `hoursOf()` / `lastSentOf()` — la date est rattachée à la première heure, sinon le rappel du matin serait rejoué le jour de la migration. **Une PWA ne peut PAS programmer une notification locale à l'avance** : l'API Notification Triggers (`showTrigger`) a été abandonnée par Google (« No longer pursuing »), jamais activée dans un navigateur stable, et un `setTimeout` dans un service worker ne survit pas à son éviction → **un serveur DOIT envoyer un push**. Chaîne : `usePushReminders` (`src/lib/usePushReminders.ts`, calqué sur `useStravaSync`) demande la permission, s'abonne (`pushManager.subscribe`, `userVisibleOnly: true`) et POSTe au **Worker Cloudflare `elan-notif`** (`worker/notif/`) un **agenda pré-calculé des 30 prochains jours** (`buildAgenda`, `src/lib/reminderAgenda.ts`) ; un **Cron Trigger** `*/5 * * * *` réveille le Worker, qui convertit l'instant courant dans le fuseau de l'abonné (`Intl.DateTimeFormat` → le passage heure d'été/hiver se gère seul, le cron étant en UTC) et pousse le rappel (`web-push-browser`, VAPID ES256 + `aes128gcm` RFC 8291). **Le Worker ne calcule JAMAIS le planning** : `buildAgenda` réutilise `canonicalCycles`/`plannedSessionIdsOn`/`planToDoOn` (mêmes fonctions qu'Aujourd'hui, `doneIds` excluant les logs `planRef` à l'identique) — le réimplémenter côté serveur créerait une seconde source de vérité vouée à diverger. Conséquence assumée : **l'agenda périme** (app pas ouverte pendant > 30 j → rappels arrêtés, repartent à la réouverture). **Identité = SHA-256 de l'endpoint push** (clé KV), pas l'`uid` Firebase : l'endpoint est une URL non devinable et EST la capacité que l'entrée protège → pas de JWT à vérifier, rappels disponibles en mode local, une entrée (donc un rappel) par appareil. `ELAN_NOTIF_KEY` en `Bearer` = anti-abus léger seulement (clé embarquée dans le bundle, donc publique), comme le Worker Strava. Le **service worker est écrit à la main** (`src/sw.ts`, stratégie `injectManifest` dans `vite.config.ts`) : il reproduit l'`autoUpdate` (`skipWaiting`/`clientsClaim`) et ajoute `push` + `notificationclick`. Typé à part (`tsconfig.worker.json`, `lib: ["WebWorker"]` incompatible avec le `DOM` de `tsconfig.app.json`, qui l'exclut). Anti-gaspillage KV : l'app ne renvoie l'agenda que si `agendaFingerprint` a changé (plan gratuit = **1 000 écritures KV/jour**). UI : bloc « Rappels de séance » des Réglages (icône Lucide `Bell`, même moule `rounded-2xl bg-sage-50 p-4` que « Courses Strava »), masqué si `VITE_NOTIF_URL` absent.

## Pièges connus

- Firestore refuse `undefined` → tout passe par `clean()` dans store.ts ; les champs optionnels vidés sont stockés comme `''`.
- `tsconfig` strict avec `noUnusedLocals` : tout import non utilisé casse le build.
- **Les CSS de `@fontsource` s'importent SANS l'extension `.css`** (`'@fontsource/space-mono/latin-400'`). Leur champ `exports` mappe `./*` vers `./*.css` : écrire l'extension demande `latin-400.css.css` et **le build vite/rolldown échoue** (« failed to resolve import »). TypeScript, lui, refuse alors l'import d'effet de bord (TS2882) — d'où le `declare module '@fontsource/*'` dans `src/vite-env.d.ts`. Les anciens imports Nunito gardent leur `.css` parce qu'ils résolvent déjà, ne pas les « harmoniser » sans revérifier le build.
- Le bundle dépasse 500 kB (Firebase) : avertissement vite normal.
- **Tout push DOIT afficher une notification.** Chrome impose `userVisibleOnly: true` et sanctionne les pushs muets : il affiche « Ce site a été mis à jour en arrière-plan », puis **révoque l'abonnement**. D'où : pas de séance aujourd'hui → le Worker n'envoie rien du tout, et `src/sw.ts` affiche un message de repli si la charge utile est illisible.
- **Sur Android, installer Avel sur l'écran d'accueil.** Pas requis pour recevoir les pushs (contrairement à iOS), mais depuis fin 2025 Chrome désabonne d'office les sites peu consultés qui notifient beaucoup — **les PWA installées en sont exemptées**.
- **Web Push depuis un Worker** : le paquet npm `web-push` ne marche PAS sur Cloudflare Workers, même avec `nodejs_compat` (il appelle `crypto.createECDH` et `https.request`). `@pushforge/builder` et `@block65/webcrypto-web-push` codent en dur l'ancien `aesgcm` (draft-04, déprécié, jamais supporté par Safari). Seul **`web-push-browser`** fait `aes128gcm` (RFC 8291) — et son option `algorithm` est **obligatoire** malgré sa doc (« defaults to AES128GCM »), sans quoi `createCEKInfo` lève « Invalid algorithm ».
- **Clé privée VAPID au format PKCS8**, pas le scalaire `d` brut : `deserializeVapidKeys` fait `importKey('pkcs8', …)`. La clé publique, elle, est au format RAW (65 octets, préfixe `0x04`) — c'est aussi l'`applicationServerKey` du navigateur.
- Régénérer les clés VAPID **invalide tous les abonnements existants** ; `scripts/generate-vapid.mjs` refuse donc d'écraser des clés présentes dans `.env`.
